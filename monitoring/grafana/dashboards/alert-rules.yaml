groups:
- name: kubernetes-alerts
  rules:
  - alert: HighPodCPU
    expr: sum(rate(container_cpu_usage_seconds_total[1m])) > 0.8
    for: 2m
    labels:
      severity: warning
    annotations:
      description: High CPU usage detected on pods
